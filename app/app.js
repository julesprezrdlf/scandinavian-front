const menu = document.getElementById('menu');
const menuContent = menu.contentDocument;

const parallaxHeader = document.getElementById('parallax-header');
const parallaxHeaderContent = parallaxHeader.contentDocument;
